import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.seacrewmanager',
  appName: 'Sea Crew Manager',
  webDir: 'dist/public'
};

export default config;
