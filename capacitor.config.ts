import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.seacrewmanager.captain',
  appName: 'Captain Voice Assistant',
  webDir: 'dist/public'
};

export default config;
